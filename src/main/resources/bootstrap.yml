app:
  appId: 000
  appName: red-packet-service
  version: 1.0.0

spring:
  profiles:
    active: local
  application:
    name: ${app.appName}
  freemarker:
    template-loader-path: classpath:/templates/
    request-context-attribute: request
    charset: UTF-8


server:
  port: 8080
